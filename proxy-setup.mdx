---
title: 'Proxy Setup'
description: 'Using a proxy to avoid rate limits during eligibility checks'
---

<Note>
  This page is a placeholder — full content coming soon.
</Note>

Topics to cover here: what the bot's proxy setting is used for (see the warning in [Configuring the Bot](/configuration#proxy) — only OpenSea sign-in and eligibility checks, not the mint transaction itself), recommended proxy providers, and how to fill in the `proxy` section of your config.
